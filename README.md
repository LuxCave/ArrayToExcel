# ArrayToExcel
This simple class converts a PHP array to an Excel file.
It is made specificaly for SuiteCRM report arrays and exports the report to a formatted ms-excel file.
It depends on "phpspreadsheet" library and creates a file formatted with greek regional defaults - decimals are separated by comma (,).
It can help as an example of using phpspreadsheet.
